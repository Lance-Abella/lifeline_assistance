class UserProfile {
  String firstname;
  String lastname;
  // Add other fields as needed

  UserProfile({
    required this.firstname,
    required this.lastname,
    // Initialize other fields
  });
}